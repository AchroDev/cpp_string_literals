#include <iostream>
#include <string>

// String Literals in C++ are a series of characters between two double quotes ""

int main()
{
    "AchroDev"; // This is a definition of a string literal // (const char[9])"AchroDev". It includes the null termination char
    std::cin.get();
}
